import { RestrictNumericDirective } from './restrict-numeric.directive';

describe('RestrictNumericDirective', () => {
  it('should create an instance', () => {
    const directive = new RestrictNumericDirective();
    expect(directive).toBeTruthy();
  });
});
